// src/utils/config.ts
// Central configuration export for easy imports across the app
import configData from '../data/config.json';

export const { company, social, google, areas, pages, marketing } = configData;
