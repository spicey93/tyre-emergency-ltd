---
import TextLayout from "../layouts/TextLayout.astro";
import { pages } from "../data/config.json";
const { cookie } = pages;
const { pageTitle, description, mainContent } = cookie;
---

<TextLayout
  pageTitle={pageTitle}
  description={description}
  mainContent={mainContent}
/>
