---
import TextLayout from "../layouts/TextLayout.astro";
import { pages } from "../data/config.json";
const { about } = pages;
const { pageTitle, description, mainContent } = about;
---

<TextLayout
  pageTitle={pageTitle}
  description={description}
  mainContent={mainContent}
/>
