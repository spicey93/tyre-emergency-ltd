---
import TextLayout from "../layouts/TextLayout.astro";
import { pages } from "../data/config.json";
const { terms } = pages;
const { pageTitle, description, mainContent } = terms;
---

<TextLayout
  pageTitle={pageTitle}
  description={description}
  mainContent={mainContent}
/>
